extends Node

## if [param already_spawned] = false, instantiates the player on spawn point. else, simply moves him to spawn point
func spawn_player(already_spawned: bool) -> void:
	pass

func set_spawn_point() -> void:
	pass
